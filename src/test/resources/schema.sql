CREATE TABLE cash_card
(
    ID     BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    AMOUNT NUMBER NOT NULL DEFAULT 0
);

SELECT u.id FROM users as u
JOIN cards as c ON  c.user_id = u.id
GROUP BY u.id
HAVING COUNT(*) > 1;